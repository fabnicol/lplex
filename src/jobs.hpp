#ifndef JOBS_HPP
#define JOBS_HPP


class jobs
{
public:

static    int notrim;
static    int seamless;
static    int discrete;
static    int padded;
static    int autoSet;
static    int continuous;
static    int backward;
static    int nearest;
static    int forward;
};

#endif // JOBS_HPP
